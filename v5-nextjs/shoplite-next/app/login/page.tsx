import { RoutePlaceholder } from "../components/RoutePlaceholder";

export default function LoginPage() {
  return <RoutePlaceholder title="Đăng nhập" description="Trang /login sẽ được bổ sung form xác thực ở module sau." />;
}
